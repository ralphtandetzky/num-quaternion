load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "norm",
    srcs = ["norm.cpp"],
    deps = [
        "@boost.qvm//:boost.qvm",
        "@eigen//:eigen",
        "@google_benchmark//:benchmark",
    ],
)
